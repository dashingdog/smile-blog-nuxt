const Config = {
  // baseUrl: process.env.NODE_ENV === 'production' ? 'https://fitapp.dashingdog.cn' : 'http://127.0.0.1:3001',
  baseUrl: 'https://fitapp.dashingdog.cn',
  staticPath: 'resource.shirmy.me'
}

export default Config
